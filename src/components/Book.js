import React from "react";

class Book extends React.Component {
     render() {
        // const { title, author, pages } = this.props.details;
        return (
            <p>book</p>
        )
    }
};

export default Book;